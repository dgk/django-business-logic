import {colorPeach, colorBlue, colorMint, colorRed, colorGrey} from "./colors";

const block_reference = {
  title: 'business_logic_reference',
  color: colorBlue
};

const block_field_get = {
  title: 'business_logic_argument_field_get',
  color: colorMint
};

const block_field_set = {
  title: 'business_logic_argument_field_set',
  color: colorMint
};

const block_function = {
  title: 'business_logic_function',
  color: colorGrey
};

const block_date = {
  title: 'business_logic_date',
  color: colorPeach
};

export {block_reference, block_function, block_field_get, block_field_set, block_date};
